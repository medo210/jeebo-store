const j=(d,s=200)=>Response.json(d,{status:s,headers:{"Cache-Control":"no-store"}});
export async function onRequestGet({env}){try{const [s,st,l,t,d]=await Promise.all([env.jeebo_db.prepare("SELECT COUNT(*) total_orders,COALESCE(SUM(total),0) total_value,SUM(CASE WHEN date(created_at)=date('now') THEN 1 ELSE 0 END) today_orders,COALESCE(SUM(CASE WHEN date(created_at)=date('now') THEN total ELSE 0 END),0) today_value FROM orders").first(),env.jeebo_db.prepare("SELECT status,COUNT(*) total FROM orders GROUP BY status").all(),env.jeebo_db.prepare("SELECT id,order_number,customer_name,product_name,total,status,created_at FROM orders ORDER BY id DESC LIMIT 8").all(),env.jeebo_db.prepare("SELECT product_name,SUM(quantity) units,COUNT(*) orders_count,COALESCE(SUM(total),0) value FROM orders GROUP BY product_name ORDER BY units DESC LIMIT 5").all(),env.jeebo_db.prepare("WITH RECURSIVE dates(day) AS(SELECT date('now','-6 day') UNION ALL SELECT date(day,'+1 day') FROM dates WHERE day<date('now')) SELECT dates.day,COUNT(orders.id) orders_count,COALESCE(SUM(orders.total),0) value FROM dates LEFT JOIN orders ON date(orders.created_at)=dates.day GROUP BY dates.day ORDER BY dates.day").all()]);return j({success:true,summary:{totalOrders:Number(s.total_orders||0),totalValue:Number(s.total_value||0),todayOrders:Number(s.today_orders||0),todayValue:Number(s.today_value||0)},statuses:st.results||[],latestOrders:l.results||[],topProducts:t.results||[],daily:d.results||[]})}catch(e){console.error(e);return j({success:false,message:"تعذر تحميل لوحة التحكم."},500)}}